# Публикует макеты вишлиста на GitHub Pages.
# Статика без сборки: содержимое репозитория выкладывается как есть.
# Корень сайта — презентация (index.html), прототип — /prototype/.
name: Deploy to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Одновременно — только один деплой; новый push отменяет незавершённый.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare site
        run: |
          mkdir -p _site
          # всё, кроме служебных папок
          rsync -a --exclude '.git' --exclude '.github' --exclude '_site' ./ _site/
          # иначе Pages прогоняет всё через Jekyll и выкидывает пути с подчёркиванием
          touch _site/.nojekyll

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
